name: reference_learning
description: A Flutter package for practicing and memorizing scripture references.
version: 0.1.0
homepage:

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.10.4
  patrol: ^4.0.1
  patrol_finders: ^3.0.0
  bdd_widget_test: ^2.1.3

flutter: